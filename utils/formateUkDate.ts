export function formateUkDate(date: Date): string {
  return date.toLocaleString('uk-UA', {
    weekday: 'long',
    year: 'numeric',
    month: 'short',
    day: 'numeric',
    hour: '2-digit',
    minute: '2-digit',
    second: '2-digit',
  });
}
